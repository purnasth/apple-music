# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...except the example, which is the documentation and holds no secret.
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# the song library: audio you drop in, plus the covers and manifest generated from it.
# Deployed as static assets straight from the build, never committed (840 MB).
/public/songs/
/public/songs-art/
/public/songs.json
